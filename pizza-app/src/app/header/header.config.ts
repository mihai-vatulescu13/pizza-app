export enum Navigation {
  Recipes = 'Recipes',
  ShoppingList = 'Shopping list',
  Authenticate = 'Authenticate',
  Logout = 'Logout',
}

export enum Manage {
  SaveData = 'Save data',
  FetchData = 'Fetch data',
  Manage = 'Manage',
}
