export enum Navigation {
  Recipes = 'Recipes',
  ShoppingList = 'Shopping list',
  Authenticate = 'Authenticate',
  Logout = 'Logout',
  Favorites = 'Favorites',
}

export enum Manage {
  SaveData = 'Save data',
  FetchData = 'Fetch data',
  Manage = 'Manage',
}
